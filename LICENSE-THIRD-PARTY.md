# Third-party dependencies

This project expects the following third-party components at build time:

- MinHook (MIT) - fetched via CMake FetchContent from https://github.com/TsudaKageyu/minhook
- SQLite3 - provided via vcpkg (`sqlite3`) or a system install discoverable by CMake
